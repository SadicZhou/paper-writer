# Docker Compose 环境变量
# 复制为 .env 后修改: cp .env.docker .env

# MySQL
MYSQL_ROOT_PASSWORD=root-secret-change-me
MYSQL_PASSWORD=db-secret-change-me

# JWT
JWT_SECRET=change-me-to-a-random-string-at-least-32-characters
